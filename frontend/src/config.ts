/**
 * 全局配置
 * 提示：此为前端运行配置，请勿与后端 .env 混淆
 */
export const config = {
  /**
   * Mock 开关：
   *  - true：后端尚未提供的接口（课程列表/详情、作业、知识库列表、问答等）
   *          改用内置逼真 mock 数据，保证界面完整可预览
   *  - false：所有请求走真实后端，缺失接口返回 404 并展示占位提示
   *
   * 后端补齐接口后，把 USE_MOCK 设为 false 即可切回真实数据，无需改动页面代码。
   */
  USE_MOCK: true,

  /** Mock 请求模拟网络延迟（毫秒），让加载态更真实，可设为 0 */
  MOCK_DELAY: 250,
}
